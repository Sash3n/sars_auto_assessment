# Claude Code project instructions

Read `AGENTS.md` before making any change. Every rule in it is binding for this
repository: git flow, commit style (no `Co-Authored-By` trailers, no em dashes),
the test-first requirement for `tax-engine` modules, README updates in every
feature PR, and the PR review checklist.
